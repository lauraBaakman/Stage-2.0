\RequirePackage{a4wide}
\RequirePackage[english]{babel}

% Macros
\RequirePackage{xspace}
\RequirePackage{xifthen}

% Images
\RequirePackage{graphicx}
\RequirePackage[hang, small,labelfont=bf,up,textfont=it,up]{caption}
\RequirePackage{subcaption}

% TEMP
\RequirePackage{todonotes}
\RequirePackage{showlabels}
\RequirePackage{layouts} % \printinunitsof{cm}\prntlen{\textwidth}

% Fonts
\RequirePackage[T1]{fontenc}

%Wiskunde
\RequirePackage{mathptmx}
\RequirePackage{siunitx}
\RequirePackage{mathtools}
\RequirePackage{amsmath}
\RequirePackage{amsfonts}
\RequirePackage{amssymb}
\RequirePackage[mathcal]{euscript}

% References
\RequirePackage{varioref}
\RequirePackage{hyperref}
\RequirePackage[noabbrev, capitalise]{cleveref}

%Lists
\RequirePackage{enumitem}
\setlist[enumerate]{leftmargin=*,label=(\roman*),itemsep=0.2ex,labelindent=\parindent}

%Tables
\usepackage{booktabs}
\renewcommand{\arraystretch}{1.2}

% Citations
% References
\RequirePackage[backend=bibtex]{biblatex}
\RequirePackage{csquotes}
\bibliography{biblio}

% Short cuts
\makeatletter
\newcommand\ie{i.e\@ifnextchar.{}{.\@}\xspace}
\newcommand\eg{e.g\@ifnextchar.{}{.\@}\xspace}
\makeatother

% Other commands
\newcommand*\justify{%
  \fontdimen2\font=0.4em% interword space
  \fontdimen3\font=0.2em% interword stretch
  \fontdimen4\font=0.1em% interword shrink
  \fontdimen7\font=0.1em% extra space
  \hyphenchar\font=`\-% allowing hyphenation
}
\renewcommand{\t}[1]{\texttt{\justify#1}}
\renewcommand{\vec}[1]{\mathbf{#1}}
\newcommand*\rfrac[2]{\ensuremath{{}^{#1}\!/_{#2}}}
\DeclareMathOperator{\diag}{diag}

\usepackage{variables}

\newcommand*\diff{\mathop{}\!\mathrm{d}}

% Distributions
\newcommand{\gaussDist}[2]{\ensuremath{\mathcal{N}\big(#1, #2\big)}}
\newcommand{\uniformDist}[2]{\ensuremath{\mathcal{U}\big(#1, #2\big)}}

% Todos
\newcommand{\beforeFinalVersion}[1]{\todo[inline, color=red!50]{\textbf{Before Final Version:} #1}}