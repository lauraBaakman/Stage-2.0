#ifndef KERNELS_DISTANCEMATRIX_IH
#define KERNELS_DISTANCEMATRIX_IH

#include "distancematrix.h"
#include <gsl/gsl_matrix.h>

double squaredEuclidean(gsl_vector* a, gsl_vector* b);

#endif //KERNELS_DISTANCEMATRIX_IH
